when:
  - event: [push, pull_request]
    branch: main

steps:
  test:
    image: python:3.11-slim
    commands:
      - pip install -e '.[dev]'
      - pytest -v

  lang_check:
    image: python:3.11-slim
    commands:
      - pip install -e .
      - triage lang --check
